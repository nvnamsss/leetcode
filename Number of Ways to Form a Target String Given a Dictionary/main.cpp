#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int numWays(vector<string>& words, string target) {
        int n = target.size();
        int m = words[0].size();
        vector<vector<int>> prefix = vector<vector<int>>();
        vector<vector<int>> dp = vector<vector<int>>(n, vector<int>(m));

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < words.size(); j++)
            {
            
            }
            
        }
    }
};