#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    int idealArrays(int n, int maxValue)
    {
        // using dp to solve this problem
        // ideal array(n) = ideal array(n-1, i + 1) 
    }
};