#include <bits/stdc++.h>
using namespace std;

struct TreeNode {

};

class Solution {
    void recursive(vector<int>& nums, vector<string>& ans, vector<bool>& visited, int target) {
        for (int i = 0; i < nums.size(); i++)
        {
            if (visited[i]) {
                continue;
            }

            
        }
        
    }
public:
    string solve(vector<int>& nums) {
        return "":
    }
};